headers = sys/wait.h
headerFilter = sys/wait.h
package = platform.linux
---

static inline int _WIFEXITED(int status) {
    return WIFEXITED(status);
}

static inline int _WEXITSTATUS(int status) {
    return WEXITSTATUS(status);
}

static inline int _WIFSIGNALED(int status) {
    return WIFSIGNALED(status);
}

static inline int _WTERMSIG(int status) {
    return WTERMSIG(status);
}
